強制的に  *UTF-8 LF*。

キーバインド

移動系
                   (C-p)
                    ^
                    |
 (C-j)<<-   (C-b)<-  ->(C-f)   ->>(C-;)
                    |
                    v
                   (C-n)

C-h 		   :back space
C-k 		   :kill-line
C-x y		   :kil-ringの中をみて貼り付け
C-c y		   :プログラムコードの補完
M-x qrr 	   :置換
C-c s		   :シェルを起動
C-c c 		   :コンパイル (後にC-x ` でコンパイルエラー箇所に飛ぶ)
C-c d 		   :デバッグ
C-x p 		   :helm(全てを統一するもの)
C-x enter 	   :短冊選択 (M-nで連番生成)
C-x l 		   :ラインに移動
C-c w 		   :無駄な空白を削除
C-c a 		   :コメントや空白を含めて美しく整形(かなり強力)
C-d r   	   :ディレクトリを開いた状態でrを押すとファイル名を編集できる
C-x C-a 	   :同じ単語をまとめて編集(カーソルを単語にのせて)
C-u 		   :いい感じに範囲指定する タイプするたびに範囲が広がる
C-x b 		   :buffer 切り替え
C-c g 		   :gitを利用する
C-x C-r		   :最近使ったファイルを開く
C-t 		   :パターンを動的に読み取って繰り返しを実行
C-e		   :選択領域に対してマクロを実行
TAB 		   :programing中にいい感じに補完する(yasnippet)
M-x recover-file   :セーブせずに終了したファイルの復元を行ってくれる(ファイルを指定すること)
C-c j		   :一時的なメモ帳を開く
byte compile
C-x dでdiredモードにして
でチェックを付けた後にBで一括バイトコンパイル


============================================================
ファイル構成
init.el		:設定ファイル
ac-comphist.dat :auto-completeの補完辞書が更新されていくところ
elpa/		:拡張機能のlispの保管場所(package-installで管理されている)
backup/ 	:全ての編集中のバックアップファイルが保存される場所(ファイル自身の情報)
auto-save-list	:全ての編集中のバックアップファイルが保存される場所(マシーンの情報)
undohist	:undoした履歴を保存しておく場所
